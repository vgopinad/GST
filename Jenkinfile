pipeline {
    agent any
    stages {
        stage('Build') {
            steps{ 
                echo 'Build'
            }
        }
        
        stage('Test') {
            steps { 
                echo 'TEST'
            }
        }
        
        stage('Deploy') {
            steps { 
                echo 'Deploy state'
            }
        }
        
    }
}
